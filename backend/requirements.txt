fastapi>=0.141.1,<1
uvicorn[standard]>=0.32,<1
sqlalchemy[asyncio]>=2.0.36,<3
aiosqlite>=0.20,<1
asyncpg>=0.30,<1
alembic>=1.14,<2
pydantic-settings>=2.6,<3
tzdata>=2024.2
argon2-cffi>=23.1,<26
# Floor is the first release past PYSEC-2026-3552/3553/3554 (a PKCS#7 decryption oracle, an
# exponential blowup resolving X.509 chains, and a name-constraint wildcard escape). This is the
# second time an upper bound here has held the package on a vulnerable line -- `<46` did it before
# and `<49` did it again -- so the ceiling exists only to match msal's `cryptography<51` and must
# be raised to follow a fix, never left to block one.
cryptography>=50,<51
httpx>=0.27,<1
PyJWT[crypto]>=2.9,<3
azure-identity>=1.19,<2
# The `azure.identity.aio` credentials acquire tokens through azure-core's async transport, which
# is aiohttp. azure-identity does not depend on it, so without this line every real Azure call
# fails at token acquisition with "aiohttp package is not installed" — Test, Discover, and every
# start or stop against a live tenant. Floor is past the CVE-2024-52304/52303 request smuggling
# and directory traversal fixes.
aiohttp>=3.11.11,<4
python-multipart>=0.0.17,<1
# SAML assertion parsing and signature verification. Wheels only - no native xmlsec build.
lxml>=5.3,<7
signxml>=4.0,<5
